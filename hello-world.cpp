#include <iostream> // This is the C++ equivalent of <stdio.h>

int main() {
    // std::cout is used for printing, similar to printf in C
    // std::endl is used to print a new line, similar to \n
    std::cout << "Hello, World!" << std::endl;
    
    return 0;
}